#include <bits/stdc++.h>
using namespace std;

int main()
{
    /*
    Working on bits is O(1) stuff
   
    Generally Data Compression
    Left Shift
    number<<no. of places;

    N<<i result is (N* 2^i)
``````````````````````````````````````````    
    Right Shift 
    Number>>number_of_places
``````````````````````````````````````````
    AND bitwise operator
    &
    x&1 is x
    x&0 is 0
    4&5 is 4

````````````````````````````````````````````
    OR operator |

    x|1 is 1
    x|0 is x

    4|5 is 5

``````````````````````````````````````````
    NOT (~)
    ~0 is 1
    ~1 is 0
    ~ x is flip of all the bits
    ~4 is -5
``````````````````````````````````````````

    XOR (^)
    x^1 is ~x
    x^0 is x
    4^5
    is 1
    
    */
   int a=8;
   a<<1

    
    
    return 0;
}
